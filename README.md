# AIOS Context Compiler

> 30/30 组真实对照实验 Token 全部下降，平均减少 50.8%。下一步，把人工精简升级成自动上下文编译。

[中文产品说明](docs/PRODUCT_BRIEF.zh-CN.md) | [English brief](docs/PRODUCT_BRIEF.en.md) | [我们正在找谁](COLLABORATE.md) | [在线演示](site/index.html)

[实验聚合数据与边界](evidence/README.md) · [Machine-readable summary](evidence/summary-valid.json)

## 直接参与

- **Runtime 方向**：评审并实现 OpenCode 每轮上下文的可开关接管、阶段隔离和完整 Trace。
- **Context / Eval 方向**：独立设计并执行能检验任务质量、上下文质量与额外成本的严格 A/B Test。

仓库会自动发布这两个首批工程任务。你不必先承诺长期合作，可以先在 Issue 中指出设计漏洞、提交一个最小 Spike，或独立复核实验协议。

不想先读完整任务，也可以从 **[一小时入门挑战](community/issues/03-one-hour-challenge.md)** 开始：只提交一个可复现的上下文绕过点，或一个能推翻方案的测试任务。

## 我们已经做到什么

在 10 类任务、30 对 A/B 实验和 66 次真实模型调用中，最小 Context Package 取得了：

- **30/30 对 A/B 实验的总 Token 全部下降**；
- **平均总 Token 减少 50.8%**；
- **禁用/无关信息泄漏率从 23.3% 降到 6.7%**；
- 全程保留请求、响应、Token 与执行时间原始记录。

这证明了一件有价值的事：编码 Agent 并不需要把全部项目知识塞进每次模型调用。更小、更受控的上下文可以显著降低成本和信息污染。

现在真正值得攻克的是：把人工准备的最小上下文，升级成模型驱动、可追溯、可验证的自动 Context Compiler。

## 为什么需要它

现在的编码 Agent 已经能读文件、调用工具并持续执行，但仍会反复出现：

- 关键规则明明存在，Agent 还是漏掉；
- 旧决策、错误经验和无关资料混入当前任务；
- `AGENTS.md`、Skills、Memory 和工具定义持续膨胀；
- Agent 不知道自己缺少信息，却能产出逻辑自洽但不符合用户意图的结果；
- 换会话或换 Agent 后，用户必须重新解释项目；
- 用户看不到每一轮模型实际收到了什么，无法判断错误来自模型还是上下文。

这不是简单的“窗口不够大”，而是模型外部知识没有被正确选择、组合、验证和更新。

## 核心假设

我们希望在 Agent 与外部知识之间增加一个 **Context Compiler**：

```text
用户任务
  -> 意图与任务契约
  -> 只查看上下文源目录和摘要
  -> 选择相关文件与片段
  -> 编译为可追溯的 Task Context Package
  -> 在受控上下文中执行任务
  -> 记录结果与写回候选
```

它不是把所有项目文件塞给模型，也不是先做一个通用 RAG。第一阶段要验证的是：

> 模型驱动的“先选择、再读取、再编译”是否能减少关键遗漏和无关干扰，同时控制 Token、耗时与用户返工。

## 第一阶段

第一阶段基于 OpenCode，接管一条可开关、可观测的上下文组装路径：

1. `Intent Pass`：只看用户消息、极简环境和源文件目录，输出任务契约与读取计划。
2. `Compile Pass`：只读取被选中的内容，生成短而可追溯的 Context Package。
3. `Execution Pass`：新执行阶段只接收任务、Context Package 和按需选择的工具/权限。
4. `Trace`：保留每轮真实输入、来源、Token、工具调用和验证结果。
5. `A/B Test`：与同版本、同模型、同任务的原生 OpenCode 严格隔离对照。

首批上下文源保持简单：

```text
AGENTS.md       稳定项目规则
HANDOFF.md      当前进度与待办
DECISIONS.md    已确认决策及理由
FILES_MAP.md    文件导航
skills/         可复用流程
hooks/          可确定性执行的约束
memory/         用户偏好、经验和错误模式
```

## 实践依据

我们从本机五类 Agent 工具、项目记录和执行轨迹中审计了 2,033 条去重用户消息，归纳出 128 个真实使用问题。上下文编译器直接面对的首批问题见[研究依据](docs/EVIDENCE.md)。

已有实验验证了“上下文精简与隔离”的价值；下一阶段将验证自动意图识别、文件选择和上下文编译能否进一步提升复杂任务的一次成功率。这不是从零开始的概念项目，而是从真实数据继续向前推进的工程问题。

## 寻找技术伙伴

优先寻找两类工程师：

- **Agent Runtime / OpenCode 工程师**：熟悉 TypeScript、Agent loop、模型消息、工具注入、插件或内核扩展。
- **Context / Evaluation 工程师**：熟悉上下文工程、信息检索、Agent eval、实验隔离和可复现实验。

可以从一次 45 分钟设计评审或一个两周原型开始，再决定开源共同维护、项目制合作或长期合伙。详情见 [COLLABORATE.md](COLLABORATE.md)。

## 当前状态

- 产品问题与核心假设：已形成
- 真实痛点审计：已完成
- 多轮原型与失败复盘：已完成
- 已验证效果：最小 Context Package 平均总 Token -50.8%，信息泄漏显著下降
- 下一目标：完成受控、可观测的自动三阶段编译器，并验证任务质量提升

本项目欢迎对方向的反驳。比“听起来很对”更重要的是一起把它做成一个可以被证伪、被测量、被复现的系统。
